# Git 部署指令

git add index.html

git commit -m "fix: 重寫圖片載入系統 + Qwen中文優化

## 主要修復

### 1. 圖片載入系統全面重寫
新增 proxyLoadImage() 使用 6 種方式依序嘗試：
1. 直接載入（crossOrigin）
2. wsrv.nl 圖片代理
3. images.weserv.nl 圖片代理
4. api.allorigins.win fetch代理
5. corsproxy.io fetch代理
6. api.codetabs.com fetch代理

每種方式 20 秒超時，自動切換下一個

### 2. Qwen-Image 中文優化
- 用雙引號包文字（手機友好）
- 自動偵測引號內文字，加入渲染提示
- 強制 response_format: b64_json
- 步數提高到 30

### 3. AI 解析可編輯
- div → textarea"

git push origin main
