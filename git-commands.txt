# Git 部署指令

git add index.html

git commit -m "fix: 重寫圖片載入 + 新增AI提示詞建議

## 主要修復

### 1. 圖片載入邏輯重寫
- 先嘗試直接載入原始URL
- 失敗則依序嘗試3個圖片代理：
  1. wsrv.nl
  2. images.weserv.nl  
  3. imageproxy.pimg.tw
- 每個方式8秒超時自動切換
- 使用Image物件測試載入（無需CORS）

### 2. 新增 AI 提示詞建議功能
- 點擊「💡 AI建議」按鈕
- 輸入簡短描述，AI生成5個優化建議
- 點擊建議即可套用
- 使用 gpt-4o-mini 快速生成

### 3. Qwen-Image 中文優化
- 用雙引號包住要顯示的文字
- 自動加入文字渲染提示
- 強制 b64_json 格式（避免CORS）
- 步數提高到30

### 4. AI 解析可編輯
- div → textarea"

git push origin main
